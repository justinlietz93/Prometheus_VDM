# VDM Nexus repo-level environment
# Resolution order: CLI flags > environment variables > .env (repo root)
# This file is read by approval and Nexus. Do not store secrets.

# Repository root for path resolution (used by Nexus and scripts)
#VDM_REPO_ROOT=C:/git/VDM/Prometheus_VDM
VDM_REPO_ROOT=/mnt/ironwolf/git/Prometheus_VDM/

# Canonical approvals databases
# Public approvals DB (read-only for runtime; writes via approve_tag.py)
VDM_APPROVAL_DB=/mnt/ironwolf/git/Prometheus_VDM/derivation/code/common/data/approval.db
# Admin DB (password hash only; used by CLI)
VDM_APPROVAL_ADMIN_DB=/mnt/ironwolf/git/Prometheus_VDM/derivation/code/common/data/approval_admin.db

# Optional: Results DB root override for Nexus Results History (read-only)
# Windows UNC example (shared across machines):
#VDM_RESULTS_DB_ROOT=\\\\192.168.0.238\\vdm_results
# Linux mount example:
#VDM_RESULTS_DB_ROOT=/mnt/vdm_results
VDM_RESULTS_DB_ROOT=/mnt/ironwolf/git/Prometheus_VDM/derivation/code/outputs/databases

# Optional: Nexus local cache DB (GUI-only; derivative, safe to delete)
#VDM_NEXUS_DB=C:/git/VDM/Prometheus_VDM/VDM_Nexus/data/nexus.db
VDM_NEXUS_DB=/mnt/ironwolf/git/Prometheus_VDM/VDM_Nexus/data/nexus.db

# Optional: Enable in-situ publish from runners when launched by Nexus
# Set by Nexus process per-run; keep disabled by default here
VDM_NEXUS=0

# Optional: Default approver display name for approvals UI/CLI
VDM_APPROVER_NAME=Justin K. Lietz

# Policy knobs (commented by default; enable if you want hard blocks)
# VDM_POLICY_HARD_BLOCK=1
# RESULTSDB_SKIP_APPROVAL_CHECK=0

VDM_DB_IP_ADDRESS=192.168.0.238