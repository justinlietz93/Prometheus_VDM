# ROCm-specific deep learning stack for AMD GPUs
# Detected GPUs: gfx908 (MI100), gfx1100 (RX 7900 XTX)
# ROCm runtime: 1.15 (likely ROCm 6.x)
#
# Usage:
#   pip install -r requirements.txt
#   pip install --index-url https://download.pytorch.org/whl/rocm6.2 torch torchvision torchaudio
#
# Alternatively, use this file directly:
#   pip install --index-url https://download.pytorch.org/whl/rocm6.2 -r requirements-rocm.txt
#
# Note: Choose the index that matches your installed ROCm (rocm6.0, rocm6.1, rocm6.2).
# MI100 (gfx908) and RDNA3 (gfx1100) are supported in ROCm 6.x wheels.

# Pinned to latest compatible wheels available on ROCm 6.2 index at the time of writing.
# You may adjust versions if necessary; leaving unpinned uses the latest on that index.
torch
torchvision
torchaudio
