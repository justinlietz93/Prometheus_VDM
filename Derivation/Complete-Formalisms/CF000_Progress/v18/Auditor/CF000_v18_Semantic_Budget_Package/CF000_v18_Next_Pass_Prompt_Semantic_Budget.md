# CF000 v18 Next-Pass Prompt — Semantic Budget / Unlocked Definitions for HRDAP

Attach this prompt together with:
- **CF000 v18 manuscript package**
- **Single_Fundamental_Primitive_Bifurcation_Law_AC_Revision_v6.md** (or the latest doctrine note if renamed)

---

v18 is now clean enough at the theorem-spine naming level that the next main danger is no longer wording sludge in the abstract.

The next main danger is:

> proving **HRDAP** by quietly importing semantics that should only become legal **after HRDAP closes**.

So this next pass should not be a frame rewrite and should not introduce new structural machinery. It should make the dependency ladder operational by explicitly controlling semantic scope.

## Required task

Please add a **semantic budget / unlocked definitions** mechanism for the HRDAP section.

For the live HRDAP burden, the manuscript should explicitly state:

### 1. Current semantic budget
What semantics are legal **before HRDAP closes**.

This should stay limited to already-earned notions at v18, such as:
- origin
- realizable
- flat / non-flat
- differentiated articulation
- invariant-bearing articulation
- lowest-resolution degree after origin
- schematic higher-resolution degree target
- lower-resolution degree not sufficient as sole determination
- additional internal determination only as character consequence *if admitted*
- dependence order
- local settlement / global non-resolution

### 2. Blocked imports
What semantics cannot be used to argue for HRDAP.

This should explicitly block:
- apartness
- contextual noncoincidence as if already closed
- nodehood
- branch / tree / graph structure
- locality
- metricity
- boundary completion
- carrier-like support
- recursive refinement as though already active
- geometric dimension language
- any unearned external relation language

### 3. Unlocked definitions if HRDAP closes
What becomes newly live only **if HRDAP closes**.

Likely:
- actual admitted higher-resolution differentiated degree
- stronger use of additional internal determination
- contextual noncoincidence as the next live burden
- apartness as a downstream candidate
- cleaner attack surface for RRP

## Important discipline

Do not change the v18 theorem structure.
Do not reopen the frame.
Do not reopen WNF.
Do not add new machinery unless the theorem forces it.

This should be a **scope-control pass**.

## Why this matters

v18 already says the dominant remaining risk is premature proof of HRDAP by borrowing recursion, locality, nodehood, or carrier-like structure. The manuscript therefore now needs a mechanism that says, section by section:

- what semantics are legal now,
- what semantics are blocked now,
- and what semantics unlock only after closure.

## Return package

Please return:
- the full revised manuscript in place as the new version,
- an updated contamination self-audit,
- an updated migration delta,
- an updated theorem inventory,
- an updated open-burden inventory,
- one short **semantic-budget note**,
- and one short **unlocked-definitions note**.

## Success condition

A successful pass makes it operationally hard to cheat on HRDAP.

The goal is not more abstraction.
The goal is a cleaner proof environment:

- current scope clearly bounded,
- blocked imports clearly named,
- unlocked semantics clearly deferred until actually earned.
