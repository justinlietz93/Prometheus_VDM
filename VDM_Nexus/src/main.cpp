#include <QGuiApplication>

int main(int argc, char* argv[]) {
  QGuiApplication app(argc, argv);
  // Placeholder entrypoint to satisfy top-level build target (physics_nexus_app).
  // UI and adapters are added after application ports/adapters compile per NEXUS §12.4.
  return 0;
}