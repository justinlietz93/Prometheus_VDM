
# VDM Nexus — Deterministic environment (example)
# Reference: [NEXUS_ARCHITECTURE.md](./NEXUS_ARCHITECTURE.md:82) and policy notes in project canon.
# Usage:
#   - Copy to .env at repo root or export in your shell before runs.
#   - Do NOT commit secrets. This file contains only non-sensitive defaults.

# Deterministic threading (set to 1 for reproducible CPU math; override per-run for perf)
OMP_NUM_THREADS=1
OPENBLAS_NUM_THREADS=1
MKL_NUM_THREADS=1
BLIS_NUM_THREADS=1
VECLIB_MAXIMUM_THREADS=1
NUMEXPR_NUM_THREADS=1
PYTHONHASHSEED=0

# Optional: Repository and approvals roots (uncomment and set if needed)
#VDM_REPO_ROOT=/mnt/ironwolf/git/Prometheus_VDM
#VDM_APPROVAL_DB=/mnt/ironwolf/git/Prometheus_VDM/Derivation/code/common/data/approval.db
#VDM_APPROVAL_ADMIN_DB=/mnt/ironwolf/git/Prometheus_VDM/Derivation/code/common/data/approval_admin.db